[package]
name = "git-wt"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "git-wt"
path = "src/main.rs"

[profile.release]
lto = "thin"

[dependencies]
clap = { version = "4", features = ["derive"] }
git2 = { version = "0.21", features = ["ssh", "https"] }
anyhow = "1"
